load("@gazelle//:def.bzl", "gazelle", "gazelle_binary")

gazelle_binary(
    name = "gazelle_bin",
    languages = [
        "@gazelle_py//py",
        "@gazelle_ts//ts",
    ],
)

gazelle(
    name = "gazelle",
    gazelle = ":gazelle_bin",
)

